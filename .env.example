PORT=4000
JWT_SECRET=change-this-secret
ADMIN_USERNAME=admin
ADMIN_PASSWORD=verdana2024

# Optional: comma-separated origins. Leave blank to allow all during demo.
ALLOWED_ORIGINS=

# Optional SMTP email settings for real confirmation emails
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
FROM_EMAIL=reservations@verdana.in
